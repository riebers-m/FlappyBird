//
// Created by Riebers on 09.02.2025.
//

#pragma once

namespace game {
static std::string const PROJECT_NAME = "@PROJECT_NAME@";
static std::string const PROJECT_VERSION = "@NanoEngine2D_VERSION@";
static std::string const GIT_VERSION = "@GIT_VERSION@";
static std::string const GIT_COMMIT = "@GIT_COMMIT@";
static std::string const BUILD_TIMESTAMP = "@BUILD_TIMESTAMP@";

static constexpr std::string version() {

    return std::string{"=========================================" \
    "\n  Project:      " + PROJECT_NAME + \
    "\n  Version:      " + PROJECT_VERSION + \
    "\n  Git Version:  " + GIT_VERSION + \
    "\n  Git Commit:   " + GIT_COMMIT + \
    "\n  Build Time:   " + BUILD_TIMESTAMP + \
    "\n====================================="};
}
}
